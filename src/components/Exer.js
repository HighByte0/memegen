function greeting(name){
    const date=new date();
    const hours=date.getHours()
    if(hours>4 && hours<11){
        return `good morning+${name}`
    }
    else{
        `sir tn3s+${name}`
    }
}